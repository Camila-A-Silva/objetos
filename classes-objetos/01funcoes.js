


// Resolução 01 função de seta (arow function)
function calcPorcentagem(valor, taxa){
    return valor * (taxa / 100);
}


let calcPagParzo = (valor, taxa) =>{
    return valor + (valor * (taxa / 100));
}


let calcPagVista = (valor, taxa) =>{
    return valor - (valor * (taxa / 100));
}